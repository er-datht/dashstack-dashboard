## 1. Pin Versions

- [x] 1.1 Remove `^` prefix from all version strings in the `dependencies` section of `package.json`
- [x] 1.2 Remove `^` prefix from all version strings in the `devDependencies` section of `package.json`
- [x] 1.3 Remove `~` prefix from the `typescript` version string in the `devDependencies` section of `package.json`

## 2. Verification

- [x] 2.1 Verify no version string in `package.json` starts with `^` or `~`
